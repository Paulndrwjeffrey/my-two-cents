<div class="outer">
  <div class="inner">
    <h2>{{ title }}</h2>
     <h3>By {{ user.name }}</h3>
  </div>
  <div class="inner">
    <p data-id="{{ id }}" class="text">{{ text }}</p>
  </div>
</div>  