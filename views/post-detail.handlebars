{{> onepost post}}

{{#if logged_in }}

<form class="comment-form">  
  <div class="comments">
      <textarea id="comment" class="comment-input"></textarea>
      <button id="comment-submit" class="button" type="submit">
        <h3>POST</h3>
      </button>
  </div>
</form>

{{/if }}

{{> comment post.Comments}}

{{#if logged_in }}
<script src="/js/comment.js"></script>
{{/if }}